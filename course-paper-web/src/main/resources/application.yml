server:
  port: 8090

spring:
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 15MB
      location: C:\Users\Кокон\IdeaProjects\course-paper\file

jwt:
  token:
    secret: ru.gold.ordance
    expirationMs: 1200000