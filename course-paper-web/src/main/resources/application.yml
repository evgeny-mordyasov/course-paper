server:
  port: 8090

spring:
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

  servlet:
    multipart:
      location: ${java.io.tmpdir}

jwt:
  secret: ru.gold.ordance
  expirationMs: 1200000


persistence:
  postgres:
    url: jdbc:postgresql://localhost:5433/docs
    username: docs
    password: qwe123